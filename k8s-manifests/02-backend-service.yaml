apiVersion: v1
kind: Service
metadata:
  name: backend-service        # <-- Frontend uses THIS DNS name to reach backend
spec:
  type: ClusterIP              # Internal only — NOT exposed to internet
  selector:
    app: vote-backend          # Routes traffic to pods with this label
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP